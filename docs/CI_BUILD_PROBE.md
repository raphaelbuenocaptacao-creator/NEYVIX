# NEYVIX CI build probe

Temporary non-production file used to trigger the pull-request validation workflow and independently verify `typecheck` + `build` outside Vercel.

Probe rerun after strict TypeScript, dependency security, Mail, Automation and access-control changes.
